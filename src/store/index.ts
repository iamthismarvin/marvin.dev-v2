import Vue from 'vue';
import Vuex from 'vuex';
import terminal from '@/store/modules/terminal';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    terminal,
  },
});
